/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/docs/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { FieldService } from "../field.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { FieldCreateInput } from "./FieldCreateInput";
import { FieldWhereInput } from "./FieldWhereInput";
import { FieldWhereUniqueInput } from "./FieldWhereUniqueInput";
import { FieldFindManyArgs } from "./FieldFindManyArgs";
import { FieldUpdateInput } from "./FieldUpdateInput";
import { Field } from "./Field";
import { SectionToFieldFindManyArgs } from "../../sectionToField/base/SectionToFieldFindManyArgs";
import { SectionToField } from "../../sectionToField/base/SectionToField";
import { SectionToFieldWhereUniqueInput } from "../../sectionToField/base/SectionToFieldWhereUniqueInput";
@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class FieldControllerBase {
  constructor(
    protected readonly service: FieldService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @nestAccessControl.UseRoles({
    resource: "Field",
    action: "create",
    possession: "any",
  })
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Field })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async create(@common.Body() data: FieldCreateInput): Promise<Field> {
    return await this.service.create({
      data: data,
      select: {
        createdAt: true,
        fieldType: true,
        fieldValue: true,
        id: true,
        title: true,
        updatedAt: true,
        validationType: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "Field",
    action: "read",
    possession: "any",
  })
  @common.Get()
  @swagger.ApiOkResponse({ type: [Field] })
  @swagger.ApiForbiddenResponse()
  @ApiNestedQuery(FieldFindManyArgs)
  async findMany(@common.Req() request: Request): Promise<Field[]> {
    const args = plainToClass(FieldFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        createdAt: true,
        fieldType: true,
        fieldValue: true,
        id: true,
        title: true,
        updatedAt: true,
        validationType: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "Field",
    action: "read",
    possession: "own",
  })
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Field })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async findOne(
    @common.Param() params: FieldWhereUniqueInput
  ): Promise<Field | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        createdAt: true,
        fieldType: true,
        fieldValue: true,
        id: true,
        title: true,
        updatedAt: true,
        validationType: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @nestAccessControl.UseRoles({
    resource: "Field",
    action: "update",
    possession: "any",
  })
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Field })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async update(
    @common.Param() params: FieldWhereUniqueInput,
    @common.Body() data: FieldUpdateInput
  ): Promise<Field | null> {
    try {
      return await this.service.update({
        where: params,
        data: data,
        select: {
          createdAt: true,
          fieldType: true,
          fieldValue: true,
          id: true,
          title: true,
          updatedAt: true,
          validationType: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @nestAccessControl.UseRoles({
    resource: "Field",
    action: "delete",
    possession: "any",
  })
  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Field })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async delete(
    @common.Param() params: FieldWhereUniqueInput
  ): Promise<Field | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          createdAt: true,
          fieldType: true,
          fieldValue: true,
          id: true,
          title: true,
          updatedAt: true,
          validationType: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "SectionToField",
    action: "read",
    possession: "any",
  })
  @common.Get("/:id/sectionToFields")
  @ApiNestedQuery(SectionToFieldFindManyArgs)
  async findManySectionToFields(
    @common.Req() request: Request,
    @common.Param() params: FieldWhereUniqueInput
  ): Promise<SectionToField[]> {
    const query = plainToClass(SectionToFieldFindManyArgs, request.query);
    const results = await this.service.findSectionToFields(params.id, {
      ...query,
      select: {
        createdAt: true,
        id: true,
        updatedAt: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @nestAccessControl.UseRoles({
    resource: "Field",
    action: "update",
    possession: "any",
  })
  @common.Post("/:id/sectionToFields")
  async connectSectionToFields(
    @common.Param() params: FieldWhereUniqueInput,
    @common.Body() body: SectionToFieldWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      sectionToFields: {
        connect: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @nestAccessControl.UseRoles({
    resource: "Field",
    action: "update",
    possession: "any",
  })
  @common.Patch("/:id/sectionToFields")
  async updateSectionToFields(
    @common.Param() params: FieldWhereUniqueInput,
    @common.Body() body: SectionToFieldWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      sectionToFields: {
        set: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @nestAccessControl.UseRoles({
    resource: "Field",
    action: "update",
    possession: "any",
  })
  @common.Delete("/:id/sectionToFields")
  async disconnectSectionToFields(
    @common.Param() params: FieldWhereUniqueInput,
    @common.Body() body: SectionToFieldWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      sectionToFields: {
        disconnect: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }
}
